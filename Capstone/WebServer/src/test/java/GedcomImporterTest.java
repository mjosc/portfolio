import org.junit.After;
import org.junit.Before;

import static org.junit.Assert.*;

public class GedcomImporterTest {

  @Before
  public void setUp() throws Exception {
  }

  @After
  public void tearDown() throws Exception {
  }

  /* TODO
   *
   * What is the best way to test the functionality of a private method called from the constructor?
   */



  //  Set<Person> vertexSet = dag.vertexSet();
  //    for (Person p : vertexSet) {
  //      System.out.println(p.getName());
  //    }
  //
  //    Person a = dag.getVertex(5);
  //    Person b = dag.getVertex(9);
  //    Set<DefaultEdge> edges = dag.getAllEdges(a, b);
  //    for (DefaultEdge edge : edges) {
  //      System.out.println(edge);
  //    }
}